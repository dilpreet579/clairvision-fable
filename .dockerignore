# Keep the build context lean and cache behavior predictable — services
# COPY only shared/, pipeline/, api/ subpaths, but the whole context is
# still hashed/sent without this file.
.venv/
.git/
.claude/
node_modules/
frontend/node_modules/
frontend/.next/
pipeline/weights/*.pth
indexes/
.env
