# Base URL of the FastAPI backend (used when mocks are off)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Serve deterministic in-browser mock data instead of calling the API.
# Default is true until the backend exists. Set to "false" to hit the real API.
NEXT_PUBLIC_USE_MOCKS=true
